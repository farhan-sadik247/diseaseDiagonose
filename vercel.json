{
  "buildCommand": "cd disease-diagnosis-frontend && npm install && npm run build",
  "outputDirectory": "disease-diagnosis-frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
